# Contributing to CallGuard AI

Thank you for your interest in contributing! We aim to maintain a high level of engineering maturity. 

## Development Process
1. **Branching**: Use `feature/issue-number-description`, `fix/issue-number`, or `docs/description`.
2. **Commits**: Follow [Conventional Commits](https://www.conventionalcommits.org/en/v1.0.0/).
   - `feat:` A new feature
   - `fix:` A bug fix
   - `docs:` Documentation only changes
   - `style:` Changes that do not affect the meaning of the code (white-space, formatting, etc)
   - `refactor:` A code change that neither fixes a bug nor adds a feature
   - `perf:` A code change that improves performance
   - `test:` Adding missing tests or correcting existing tests
   - `chore:` Changes to the build process or auxiliary tools and libraries
3. **Pull Requests**: Ensure all tests pass. Update documentation where relevant.

## Local Setup
See the [README.md](README.md) for local docker-compose setup instructions.
